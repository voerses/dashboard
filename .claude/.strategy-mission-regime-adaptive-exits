status: active
goal: Research and implement regime-conditional exit strategies. In bear markets, altcoin spikes are functionally mean-reversion events — treat them as such with fixed targets, fast scale-outs, and time stops instead of trailing. In bull markets, let winners run with wide trails. The exit PARADIGM should change with regime, not just exit parameters.
started: 2026-03-13
type: research+implementation
trigger: Observed quick altcoin spike producing large unrealized gains that could reverse. Current exits (progressive ATR trail) are optimized for trending markets, not bear-market spikes.
baseline:
  current_exits: progressive ATR trail (3.0→1.5 ATR based on profit), time trail (s69/s72), partial TP (s76), fixed TP (s75)
  exit_types_tested: 7 (stop, target, regime, rsi, mean_target, max_hold, funding)
  previous_mission: profit-taking optimization (closed 2026-03-12, deployed s69/s72/s75/s76)
  gap: ALL current exits use same paradigm regardless of macro regime
  breakeven_deployed: BE=0.5 ATR universal default (2026-03-13). 16/16 portfolios improved, median PnL +82%.
progress: |
  COMPLETED:
    Phase 1: ✅ Bear vs bull exit behavior analysis (DOWNTREND captures only 33.8% of peak PnL)
    Phase 2: ✅ Breakeven ratchet — BE=0.5 deployed universally, +52-97% PnL improvement
    Phase 3: ✅ Regime-conditional targets — bear_tgt_3.0 adds +15%, not yet deployed
    Phase 5: ✅ Comprehensive ranking of all 28 strategies × 4 periods

  REMAINING:
    Phase 4: Chandelier exit (trail from highest high) — NOT TESTED
    Phase 5: Volume-spike exit (climax detection) — NOT TESTED
    Phase 6: Acceleration-based trail (Parabolic SAR) — NOT TESTED
    Phase 6: Triple Barrier (bear-specific tight barriers) — NOT TESTED
    Phase 6: Best combo of all above — NOT TESTED
    Phase 7: Deploy best combo to paper trading — PENDING
done_when: |
  1. ✅ Documented bear-market vs bull-market exit behavior across existing strategies
  2. PARTIAL — Tested breakeven variants + bear targets, but NOT Chandelier/volume/acceleration/triple barrier
  3. ✅ Measured improvement in Calmar/Sharpe specifically in bear-market months
  4. ✅ At least one approach shows >10% Calmar improvement — BE=0.5 shows +52-97% PnL
  5. ✅ Breakeven deployed universally. Remaining approaches need testing + deployment.
  6. N/A — improvement confirmed for breakeven; other approaches still need evaluation
kill_criteria: |
  1. All regime-conditional exits degrade overall Calmar vs current approach
  2. Bear-market improvement comes at cost of >20% bull-market return reduction
  3. Added complexity doesn't justify improvement (<5% Calmar delta)
  4. Overfitting detected: in-sample gains don't hold on walk-forward test
notes: |

  ======================================================================
    REGIME-ADAPTIVE EXIT STRATEGIES MISSION
    Started: 2026-03-13
  ======================================================================

  === THE CORE INSIGHT (from literature) ===

  In bear markets, altcoin spikes are functionally MEAN-REVERSION events
  dressed up as momentum entries. The price spikes, then reverts. Our
  current trailing stops are designed for TREND-FOLLOWING — let winners
  run. But in a bear macro, there's nowhere to run TO.

  The highest-impact change is not tweaking trail multipliers — it's
  SWITCHING THE EXIT PARADIGM based on regime:
  - Bear: fixed targets, fast scale-outs, time stops (capture the spike)
  - Bull: wide trails, let winners run (ride the trend)

  This is supported by:
  - Palazzi 2025: adaptive trailing + vol filter → Sharpe 2.24 in bear
  - Han et al: stop-loss overlay → positive returns in momentum crashes
  - Li & Liu: optimal exits depend on price PATH, not just level
  - Kevin Davey (378K backtests): simple exits beat complex ones
  - Polakow (2M backtests): random exits match stop exits in bear markets
  - Risk-Managed Momentum Index: DD duration 517→29 days with regime switch

  === WHAT WE ALREADY HAVE ===

  Exit mechanisms in the engine (v4/simulator.py):
  1. Progressive ATR trail:    [0→3.0, 1→2.5, 2→2.0, 3→1.5] ATR
  2. Time-decayed trail:       [24→2.5, 30→2.0, 36→1.5, 48→1.0] (s69)
  3. Partial TP:               50% close at +2.0 ATR, trail rest at 1.5 (s76)
  4. Fixed TP:                 Exit at +3.0 ATR (s75)
  5. Funding ceiling:          KILLED — degrades at every threshold
  6. Regime exit:              Exit when regime enters exit_regimes set
  7. RSI exit:                 Exit when RSI > 70 (longs)
  8. Max hold:                 Exit after N bars

  What we DON'T have:
  - Regime-conditional trail/target switching (same exits in all regimes)
  - Breakeven ratchet (move stop to entry after +X profit)
  - Chandelier exit (trail from highest high, not current price)
  - Volume-spike exit (climax detection on volume > 150% of avg)
  - Acceleration-based trailing (Parabolic SAR-inspired tightening)
  - Triple Barrier framework (simultaneous profit/stop/time barriers)
  - Bear-specific exit: tight target + fast scale-out + short time stop

  === RESEARCH DIRECTIONS ===

  R1. REGIME-CONDITIONAL EXIT PARADIGM (highest priority)
  ──────────────────────────────────────────────────────
  Switch the entire exit approach based on per-token or market regime.

  Bear macro exits:
  a) Tight fixed TP: 1.0-1.5x ATR target (capture spike, don't wait)
  b) Aggressive scale-out: 50% at +0.75 ATR, trail rest at 1.0 ATR
  c) Short time stop: exit after 8-12 bars if no TP hit
  d) Combination: Triple Barrier with tight profit/time barriers

  Bull macro exits (current approach, validated):
  a) Wide ATR trail: 2.5-3.0x ATR, let winners run
  b) Minimal scale-out: 25% at +2R if any
  c) Long time stop: 48+ bars safety net only

  Implementation options:
  A1. Strategy-level: each strategy chooses exits based on regime
      - Simplest: add REGIME_EXIT_CONFIG dict to strategy
      - trail_schedule_by_regime = {UPTREND: wide, DOWNTREND: tight}
  A2. Engine-level: simulator picks exit config from regime
      - More generic, works for all strategies automatically
      - Needs new StrategyResult field: exit_config_by_regime
  A3. Overlay: new wrapper strategy that modifies exit params
      - No engine changes, follows AIPIP-0018 pattern
      - Override trail_schedule, target_mult based on current regime

  R2. BREAKEVEN RATCHET (most supported in literature)
  ──────────────────────────────────────────────────────
  After trade reaches +X profit, move stop to entry price (breakeven).
  Creates "risk-free" trades that can only win or break even.

  Mechanics:
  - Trigger: unrealized PnL > breakeven_atr * ATR (e.g., 0.5x ATR)
  - Action: set stop_price = entry_price + tiny buffer
  - After: normal trail logic continues from breakeven floor

  Variants to test:
  a) BE at +0.5 ATR (aggressive — locks in fast)
  b) BE at +1.0 ATR (moderate)
  c) BE at +1.5 ATR (conservative — more room to breathe)
  d) Regime-conditional: BE at +0.5 in bear, +1.0 in bull

  Implementation: add breakeven_atr field to Position, check in
  _process_exits() before trail logic. One-time flag like partial_closed.

  R3. CHANDELIER EXIT (best for spike capture)
  ──────────────────────────────────────────────────────
  Trail from the highest high, not from current price.
  stop = highest_high(N) - multiplier * ATR(N)

  Key difference from current trail:
  - Current: stop follows price, moves UP as price rises
  - Chandelier: stop follows PEAK, tightens as price FALLS from peak
  - On a spike-and-reversal: Chandelier tightens much faster

  Variants to test:
  a) Chandelier(10, 2.0 ATR) — tight, good for spikes
  b) Chandelier(20, 2.5 ATR) — moderate
  c) Chandelier(10, 1.5 ATR) in bear, Chandelier(20, 3.0 ATR) in bull
  d) Hybrid: Chandelier + progressive tightening from current system

  Implementation: modify trail logic in _process_exits() to reference
  highest_high over lookback window instead of just pos.highest.

  R4. VOLUME-SPIKE EXIT (climax detection)
  ──────────────────────────────────────────────────────
  Exit when volume spikes above threshold during a winning trade.
  Volume climax = exhaustion = likely reversal.

  Mechanics:
  - Monitor volume vs rolling average (e.g., 20-bar SMA)
  - If volume > 1.5x average AND trade is profitable → exit
  - Optional: only in bear regime (bull volume spikes can be continuation)

  Variants:
  a) Volume > 1.5x avg + profitable → full exit
  b) Volume > 2.0x avg + profitable → 50% exit (scale-out on climax)
  c) Volume > 1.5x avg → tighten trail to 1.0 ATR (don't exit, just tighten)

  Implementation: add volume_exit_mult field to StrategyResult, check
  in _process_exits() using bar volume from OHLCV data.

  R5. ACCELERATION-BASED TRAIL (Parabolic SAR-inspired)
  ──────────────────────────────────────────────────────
  Trail that tightens as the move extends (acceleration factor).
  Inspired by Parabolic SAR but adapted for our framework.

  Mechanics:
  - Start with wide trail (3.0 ATR)
  - Each bar that makes a new high: reduce trail by acceleration factor
  - AF starts at 0.02, increments by 0.02, max 0.20
  - Effect: on a parabolic spike, the trail catches up fast

  Advantage over current progressive trail:
  - Current: tightens based on PROFIT (discrete thresholds)
  - Acceleration: tightens based on TIME making new highs (continuous)
  - Better for parabolic moves where profit accumulates rapidly

  Implementation: add acceleration fields to Position, modify trail
  calculation to include SAR-style tightening.

  R6. TRIPLE BARRIER (bear-market specific)
  ──────────────────────────────────────────────────────
  Three simultaneous exit barriers per Lopez de Prado:
  - Upper: profit target (1.0-1.5x ATR in bear)
  - Lower: stop-loss (2.0x ATR)
  - Vertical: time barrier (8-12 bars in bear, 48+ in bull)
  Whichever is hit first triggers the exit.

  This naturally handles bear-market spikes:
  - Spike hits profit barrier fast → captured
  - Reversal hits stop → limited loss
  - Goes sideways → time barrier frees capital

  Implementation: already partially supported via target_mult + stop +
  max_hold. Need to make them regime-conditional and use tighter values.

  === EVALUATION METHODOLOGY ===

  For each exit variant:

  1. BEAR-PERIOD TEST
     - Run s56 (or s58 base) on Feb-Mar 2026 (confirmed bear period)
     - Measure: closed trade PnL, win rate, avg win, avg loss
     - Compare vs current exits on same period

  2. BULL-PERIOD TEST
     - Run same strategy on May-Aug 2024 (confirmed uptrend)
     - Ensure bear exits don't destroy bull performance
     - Acceptable degradation: <10% return reduction

  3. FULL-PERIOD ROBUSTNESS
     - 12-month backtest covering multiple regimes
     - Calmar, Sharpe, MaxDD must improve or hold vs baseline
     - Walk-forward validation: 2020-2024 train, 2024-2026 test

  4. PORTFOLIO IMPACT (critical — learned from s76 mission)
     - Test in multi-strategy context (4-edge, super5-dyn)
     - Overlays are portfolio-composition dependent (L2 from previous)
     - Solo improvement means nothing if it degrades the combo

  5. TRADE-LEVEL ANALYSIS
     - For each exit type: histogram of exit reasons
     - What % of trades exit at each barrier?
     - Are the best trades (big winners) being cut too early?
     - Distribution of trade P&L: did we lose the long tail?

  === IMPLEMENTATION PLAN ===

  Phase 1: Characterize Bear vs Bull Exit Behavior (< 1 hour)
  - Analyze existing trade data: exit reasons, hold times, P&L by regime
  - Identify how many trades hit trail stop vs time stop vs target
  - Measure: in bear months, how much profit is given back after peak?
  - This tells us the SIZE of the opportunity

  Phase 2: Breakeven Ratchet (< 1 hour)
  - Implement in simulator.py (small change, low risk)
  - Test BE at 0.5, 1.0, 1.5 ATR on s56 in bear + bull periods
  - Expected: most universally positive, supported by literature

  Phase 3: Regime-Conditional Exits via Overlay (< 2 hours)
  - Create s83: s56 + regime-conditional exit overlay
  - Bear: target=1.5 ATR, time_stop=12 bars, trail=1.5 ATR
  - Bull: target=999, time_stop=48 bars, trail=3.0 ATR (current)
  - Test on 12-month period, measure per-regime performance

  Phase 4: Chandelier Exit (< 1 hour)
  - Modify trail logic to use highest-high lookback
  - Test Chandelier(10, 2.0) vs current progressive trail
  - Particularly test on bear-market altcoin spikes

  Phase 5: Volume-Spike Exit (< 1 hour)
  - Add volume climax detection to exit pipeline
  - Test as standalone + combined with Chandelier/regime-conditional
  - May not need engine change — can be signal-level in strategy

  Phase 6: Best Combo (< 1 hour)
  - Combine best 2-3 approaches from Phase 2-5
  - E.g., breakeven ratchet + regime-conditional + volume climax
  - Test in multi-strategy portfolio context
  - Walk-forward validation

  Phase 7: Deploy & Monitor
  - Deploy to paper trading (new overlay on s58 pool)
  - Compare vs non-overlay pools over 50+ trades
  - Kill if: worse than baseline after 50 closed trades

  === RESULTS (2026-03-13) ===

  PHASE 1: EXIT BEHAVIOR ANALYSIS
  ─────────────────────────────────
  s56, 12mo, 805 trades:
  - DOWNTREND winners capture only 33.8% of peak unrealized PnL
  - 42 DOWNTREND losers were once profitable → $578K left on table
  - If 80% capture: +$471K additional in DOWNTREND alone
  - ALL regimes: 34.2% capture rate, $1.05M upside at 80%

  PHASE 2: BREAKEVEN RATCHET ★★★ CLEAR WINNER ★★★
  ────────────────────────────────────────────────────
  s60, 12mo, 200K:
    Baseline:  PnL $7.9M, payoff 2.67x, MaxDD 4.5%
    BE=0.5:    PnL $15.6M (+97%), payoff 5.57x, MaxDD 3.8% ← BEST
    BE=1.0:    PnL $12.7M (+61%), payoff 4.26x, MaxDD 4.1%
    BE=1.5:    PnL $9.9M (+25%), payoff 3.49x, MaxDD 4.4%

  s56, 12mo, 200K:
    Baseline:  PnL $755K, payoff 3.22x, MaxDD 3.7%
    BE=0.5:    PnL $1.15M (+52%), payoff 7.25x, MaxDD 2.5% ← BEST

  Mechanism: 13.7% of trades now exit at breakeven instead of full stop
  loss. Capital freed is redeployed into new trades.

  PHASE 3: REGIME-CONDITIONAL TARGET
  ─────────────────────────────────────
  s60, 12mo, 200K (all include BE=0.5):
    BE only:          PnL $15.6M, payoff 5.57x, MaxDD 3.8%
    BE+bear_tgt_2.0:  PnL $16.4M (+5%), payoff 4.59x, MaxDD 3.9%
    BE+bear_tgt_3.0:  PnL $17.9M (+15%), payoff 4.98x, MaxDD 3.8%
    BE+all_tgt_2.0:   PnL $19.5M (+25%), payoff 3.99x, MaxDD 3.1%

  Conclusion: bear-only target adds modest value. Universal target helps
  more but risks cutting bull winners. bear_tgt_3.0 is the safe choice.

  s56: bear target has <1% effect (s56 doesn't trade in DOWNTREND)

  RECOMMENDATION FOR DEPLOYMENT:
    breakeven_atr = 0.5  (proven, massive improvement, no downside)
    bear_target_mult = 2.0 or 3.0  (marginal benefit, low risk)

  PHASE 4: UNIVERSAL BREAKEVEN DEPLOYMENT ★★★ DEPLOYED ★★★
  ─────────────────────────────────────────────────────────
  Tested breakeven_atr=0.5 across ALL 16 portfolios (12mo, $200K each):

  Results — 16/16 improved:
  | Portfolio     | Baseline PnL | BE=0.5 PnL  | Improvement | DD Change |
  |---------------|-------------|-------------|-------------|-----------|
  | s60           | $7.9M       | $15.6M      | +97%        | -0.7pp    |
  | s69           | $755K       | $1.15M      | +52%        | -1.2pp    |
  | s72           | $2.0M       | $3.9M       | +95%        | -0.2pp    |
  | 4-edge        | $6.3M       | $6.5M       | +3.8%       | -0.1pp    |
  | super5-dyn    | $9.1M       | $12.3M      | +35%        | -8.3pp    |
  | (12 more...)  | all improved | median +82% | all +       | 15/16 +   |

  Changed defaults in 5 files (breakeven_atr 0.0 → 0.5):
  - v4/engine.py: StrategyResult.breakeven_atr default
  - v4/signals.py: TokenSignals.breakeven_atr default + extraction fallback
  - v4/position.py: Position.breakeven_atr default
  - v4/portfolio_signals.py: extraction fallback
  - v4/paper_state.py: deserialization fallback

  Removed s83-BE and s84-BE from paper config (redundant — all strategies
  now have breakeven by default). Paper trader restarted: PID 341250.

  PHASE 5: COMPREHENSIVE PORTFOLIO RANKINGS
  ──────────────────────────────────────────
  Ranked all 28 strategies × 4 periods (5yr/12mo/3mo/1mo), each fresh $200K.

  ALL TIME (~5yr) top 3:
    1. 4-edge-conv:  +1,901,656%  DD -5.7%
    2. 4-edge+ptp:   +1,886,215%  DD -4.7%
    3. s58+s65:      +1,836,961%  DD -6.3%

  LAST 12 MONTHS top 3:
    1. s60:          +16,013%     DD -7.2%
    2. s58+s60:      +12,956%     DD -4.2%
    3. super5-dyn:   +11,536%     DD -34.5%

  MARCH 2026 top 3:
    1. s60:          +109%        DD -0.7%
    2. super5-dyn:   +103%        DD -0.2%
    3. super5-conv:  +100%        DD -2.1%

  All 28 strategies profitable across all 4 periods.
  Full results: results/v4/portfolio_rankings.json

  === REMAINING RESEARCH (NOT TESTED — available for future mission) ===

  R3. Chandelier exit (trail from highest high)
  R4. Volume-spike exit (climax detection)
  R5. Acceleration-based trailing (Parabolic SAR-inspired)
  R6. Triple Barrier (bear-specific tight barriers)
  R1. Full regime-conditional exit paradigm (bear targets marginally positive)

  These are lower priority since breakeven captured the lion's share of
  the improvement. Revisit if paper trading shows bear-market weakness.

  === KEY PAPERS ===

  - Palazzi 2025: adaptive trailing + vol filter, Sharpe 2.24 in bear
  - Han et al: stop-loss overlay on momentum → positive in crashes
  - Li & Liu: optimal exits depend on price path, not just level
  - Kevin Davey: 378K backtests, simple exits beat complex ones
  - Lopez de Prado: Triple Barrier Method
  - Gradzki 2025: Triple Barrier + deep learning on BTC/ETH
  - Risk-Managed Momentum Index: DD duration 517→29 days with regime switch

  === FILES MODIFIED ===

  Engine (breakeven ratchet — deployed):
  - v4/simulator.py: breakeven check in _process_exits() (lines 397-408)
  - v4/position.py: breakeven_atr + breakeven_triggered fields
  - v4/signals.py: breakeven_atr on TokenSignals + extraction
  - v4/portfolio_signals.py: breakeven_atr extraction fallback
  - v4/paper_state.py: breakeven_atr serialization/deserialization
  - v4/engine.py: StrategyResult.breakeven_atr default

  Infrastructure (updated 2026-03-14):
  - Runner: PID 381557 (restarted on v5.0 code)
  - v3→v4 migration COMPLETE — all imports now v4, v3/ is frozen legacy
  - Data pipeline: v5.0 with 4 adversarial review rounds, 627 tests

  Testing tools created:
  - v4/test_breakeven_all_portfolios.py: batch test across all portfolios
  - v4/rank_all_portfolios.py: comprehensive multi-period ranking

  Not modified (future work if needed):
  - Chandelier exit, volume-spike exit, acceleration trail
  - Regime-conditional exit paradigm (deferred — marginal benefit)

  === KEY LEARNING ===

  L8. BREAKEVEN RATCHET IS UNIVERSALLY POSITIVE
      Unlike other overlays that are portfolio-composition-dependent (L2),
      breakeven works for EVERY strategy and EVERY combination tested.
      It has zero negative interactions because it only converts losers
      to scratch — never reduces the upside of winners. This makes it
      the only exit improvement that should be DEFAULT-ON for all strategies.

  L9. RANKINGS SHOW STRATEGY-PERIOD DIVERGENCE
      All-time best (4-edge-conv) ≠ recent best (s60). Rankings shift
      dramatically across timeframes. Implication: portfolio allocation
      should weight recent performance more than all-time, or use
      regime-adaptive allocation (dynamic weights already doing this).

  === CONSTRAINTS ===

  - Total new parameters: 1 (breakeven_atr = 0.5) — minimal overfitting risk
  - Works on ALL 138 tokens — tested across 28 portfolios
  - Backward-compatible: default changed from 0.0 to 0.5, can override per-strategy
  - Bear/bull definition: existing regime detection used for analysis
    - Regime improvement is separate mission (.strategy-mission-regime-detection)
